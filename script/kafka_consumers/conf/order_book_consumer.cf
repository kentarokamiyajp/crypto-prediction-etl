# Kafka Consumer config
TOPIC_ID="crypto.order_book"
GROUP_ID="order-book-consumer"
OFFSET_TYPE="earliest"

# Cassandra config
KEYSPACE="crypto"
TABLE_NAME="order_book_realtime"

# Set max retry count for cassandra operation
RETRY_COUNT=10000
