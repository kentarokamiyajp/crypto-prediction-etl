# Kafka Consumer config
TOPIC_ID="crypto.market_trade"
GROUP_ID="market-trade-consumer"
OFFSET_TYPE="earliest"

# Cassandra config
KEYSPACE="crypto"
TABLE_NAME="market_trade_realtime"

# Set max retry count for cassandra operation
RETRY_COUNT=10000
